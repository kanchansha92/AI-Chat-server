PORT=5000
NODE_ENV=development

DATABASE_URL="postgresql://postgres:root@localhost:5432/ember_db"

# Generate your own with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# The server refuses to start in production with a missing/default/short
# secret — see config/assertEnv.js.
JWT_SECRET="replace-with-a-long-random-value"


ADMIN_EMAILS="kanchanyahoo@gmail.com"

# Comma-separated list of allowed frontend origins.
CORS_ORIGIN="http://localhost:5173,http://localhost:4173"

TRUST_PROXY_HOPS=1

GOOGLE_CLIENT_ID="89189424954-tgfs3hg4tsdg82tdovmgitj61l5fji0v.apps.googleusercontent.com"

# Facebook developer console → your App → Settings → Basic.
FACEBOOK_APP_ID="1352433299729937"
FACEBOOK_APP_SECRET="4dc51c497e947008f2df5db0f7667cce"

# ─── Email (Resend, brief §8) ─────────────────────────────────────────────────
# Transactional mail — welcome, receipt, password reset, trial ending — all go
# out through Resend (lib/email.js), called over its REST API so there's no npm
# package to install.
# Leave RESEND_API_KEY blank and the app runs in "log-only" mode: it prints what
# it would have sent instead of sending, so local dev works without a key.
RESEND_API_KEY=""
# From: must be an address on a domain you've verified in Resend.
EMAIL_FROM="ember <hello@ember.app>"
# Reply-to: the welcome mail promises "a real person reads it" — point this at an
# inbox someone actually watches. Leave blank to omit the header.
EMAIL_REPLY_TO="hello@ember.app"
# Base URL of the web app, used to build links in emails (password reset, etc.).
# Falls back to the first CORS_ORIGIN, then http://localhost:5173.
APP_URL="http://localhost:5173"

# ─── AI (chat) — any OpenAI-compatible provider ─────────────────────────────
# FREE default via Groq (console.groq.com). Blank key → built-in stand-ins.
OPENAI_API_KEY=""
OPENAI_BASE_URL="https://api.groq.com/openai/v1"
OPENAI_MODEL="llama-3.3-70b-versatile"
# OPENAI_MODEL_VOICE="" / OPENAI_MODEL_UTILITY="" / LLM_TIMEOUT_MS=20000

# ─── Images — FREE via Pollinations (no key). "standin" forces the SVG stand-in.
IMAGE_PROVIDER="pollinations"
